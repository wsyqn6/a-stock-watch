# 更新日志

## [Unreleased]

### 新增

- 大幅异动通知：自选股涨跌幅越过阈值时弹右下角通知，可点「查看走势」直达分时图；后台常驻，侧边栏隐藏也生效，轮询用独立 15 秒慢间隔不跟侧边栏快刷
- 新增配置 `aStockWatch.bigMoveAlert`（总开关，默认关闭）、`aStockWatch.bigMoveAlertPct`（阈值，默认 5%）、`aStockWatch.bigMoveAlertCooldownMin`（冷却分钟，默认 30，同股同向冷却期内不重复弹）

## [0.1.10] - 2026-08-10

### 修复

- K线十字光标不再带中空圆环盖住蜡烛实体，改为完整虚线十字
- 分时大图增量渲染短路误吞 K线 tab 切换，日K / 周K / 月K 现在即时出图
- 切换自选股后分时布局指纹残留，偶发新标的分时图不渲染
- 自选列表警告条整树重解析导致行点击 / 删除事件丢失

### 改进

- 均价线改实线金，与昨收虚线基准一眼区分；量能柱降透明度，主图更清晰
- 昨收基准线增强：加粗加深，左端标注「昨收 xx.xx」
- 价格线末端加收盘圆点标记；指标区固定双行四槽，缺失字段以占位符对齐
- 十字光标改虚线并加水平线，tooltip 移到线旁不遮线；悬停帧率优化（rAF + 缓存布局 + 二分查找）

### 工程

- 自选列表改为增量更新（行情刷新只改文本不重建 DOM），合并冗余推送
- 分时大图刷新只更新文本与坐标，图表结构不重建，十字线绑定保持
- K线请求量由 300 根降为实际所需的 60 根；分时缓存加容量上限防无界增长

## [0.1.9] - 2026-08-09

### 修复

- 指数（上证指数、深证成指）K线图无数据：指数无复权，接口只返回 `day/week/month` 键，解析现在兼容回退
- K线数据少时单根蜡烛撑满整图：限制蜡烛最大宽度，刻度按像素间隔稀疏化防 x 轴文字重叠
- 手动排序拖入重复代码导致列表残留重复项

### 改进

- 详情页指标区键值紧凑展示，贴合雪球风格，消除键值间距过宽
- 详情页标签激活态、数据区对齐、主图悬停微交互增强
- 超过万亿的市值 / 成交额以「万亿」为单位显示
- 自选股迁移后清理旧版 globalState 残留

### 工程

- 抽公共 `util.ts` 合并 webview nonce 生成，改用 crypto UUID
- 抽取图表比例尺与排序函数，减少重复代码
- 开启 `noUnusedLocals` / `noUnusedParameters` 编译检查，清理死代码
- 补充 watchlist 排序去重测试

## [0.1.8] - 2026-08-08

### 工程

- 侧边栏样式抽离为独立 CSS 常量，webview 渲染更清晰
- 分时刷新在自选股为空时提前返回，避免空转
- 清理无用图标文件，减小安装包体积

## [0.1.7] - 2026-08-07

### 新增

- 老板模式：开启后侧边栏、分时面板、状态栏涨跌红绿全部灰显，低调不显眼
- 开关方式：侧边栏标题栏图标（眼睛 / 闭眼）、命令 `a-stock-watch.bossMode`、快捷键 `Ctrl+Alt+L`、设置项 `aStockWatch.bossMode`，状态写入配置重启保留

### 修复

- 指数分时大图折线消失：指数量额为全市场聚合，`成交额 / 成交量` 算出的"均价"与指数点位相差百倍，拖垮 y 轴比例把折线压成顶部一线；现按量纲自动识别，均价越界时不渲染均价线、价格折线恢复正常满高
- 分时大图 y 轴最高刻度数字被裁剪：标签改 `dominant-baseline="hanging"` 顶对齐，不再上探出 SVG 视口

## [0.1.6] - 2026-08-07

### 新增

- 点击自选股行在编辑器面板打开雪球风格分时大图：分时线、均价线（黄虚线）、昨收基准线、成交量柱、十字光标悬停查看时间 / 价格 / 均价 / 成交量
- 面板顶部展示名称代码、现价、涨跌额涨跌幅、今开最高最低昨收、成交量成交额
- 分时详情 10 秒自动刷新（仅交易时段），复用 60 秒分时缓存，关闭面板即释放资源

### 工程

- 分时数据解析扩展累计成交量与成交额字段，均价线按累计额 / 累计量计算

## [0.1.5] - 2026-08-07

### 新增

- 状态栏显示自选股行情：从自选股挑选最多 3 只，编辑模式下点击图钉按钮添加 / 移除
- 状态栏条目名称白字、涨跌幅红绿配色，随明暗主题自适应，点击聚焦侧边栏
- 新增配置 `aStockWatch.statusBar`（可直接在设置中编辑）

### 改进

- 添加自选股输入框：空白时失焦自动关闭，输入内容时保持打开不丢搜索词

## [0.1.4] - 2026-08-06

### 新增

- 分时图下增加同花顺式面积渐变阴影：闭到折线自身最低点，与昨收基线无关，任意走势形态可用
- 未获取到行情的自选股以黄色提示条列出（如代码无效 / 未收录）
- 搜索自选股时腾讯源无结果自动改用东方财富源兜底
- 悬浮自选股行高亮，折线加粗与阴影微加强

### 改进

- 明暗主题切换改为 CSS 变量自动跟随（删除 JS 探测与字段）
- 分时数据 60 秒 TTL 缓存，轮询请求量下降约 87%
- 周末 / 节假日休市自动跳过分时拉取

### 工程

- 清理死代码与冗余 activationEvents 声明
- 合并重复的可见性监听，修复 RefreshManager 泄漏
- .vscodeignore 补全 .vscode/ 与 bun.lock

## [0.1.3] - 2026-08-05

### 新增

- 分时图增加昨收基准线（虚线），涨跌相对昨收一目了然
- 股票名称列固定宽度，分时图起点对齐
- 超长股票名智能缩小字号显示（如半导体ETF国联安）
- 分时图加载前预留占位，行布局不再跳动

### 改进

- 收盘后 / 盘前仍显示当日分时图（不再随交易时段消失）
- 右列层级调整：涨跌幅放大为主读数字，现价缩小为配角

### 修复

- 行情与分时数据并发竞态导致分时图缺失

### 工程

- 统一命名为「A股速览」（扩展名 / 侧边栏 / 活动栏）

## [0.1.0] - 2026-08-05

首个版本。

### 功能

- 侧边栏 A 股自选股实时行情：价格、涨跌额、涨跌幅，涨跌红绿区分
- 卡片内嵌当日分时迷你走势图
- 添加自选股支持按代码 / 拼音缩写 / 名称搜索
- 拖拽手动排序，支持按代码 / 名称 / 涨跌幅自动排序
- 删除按钮与自选股管理命令
- 自选股列表保存在用户设置中，重启不丢失
- 涨跌停股票以特殊图标标识
- 主题自适应单色活动栏图标

### 低开销设计

- 仅在交易时段请求行情，休市 / 午休 / 节假日自动跳过
- 侧边栏隐藏即停止轮询，零后台开销
- 全部自选股合并为单次批量请求
- 内置防并发刷新与分时数据缓存

### 工程

- 全部行情请求改用 HTTPS，带超时与并发保护
- MIT 协议、README 与文档
